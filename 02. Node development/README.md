# Node Development

for initialize the project with node should be use the next command `npm init`, it give us some specifications to fill out it is for custom the settings, later it generate the next file `package.json`
